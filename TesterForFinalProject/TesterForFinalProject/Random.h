#pragma once

#include <random>

// for placing lines in random positions in window
namespace Random
{

    // define function get() that takes two int args
    // returns int between the arguments
    int get(int min, int max);

}